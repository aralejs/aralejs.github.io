// TODO 1. 根据指定的rootElement, parse出组件层面信息， 可以动态组件初始化。
// TODO 2. 根据指定的view, parse出里面相关数据。(是否支持handlebars的方式， 支持action的绑定？)
define(function(require, exports, module) {
    var Class = require('class');
    var DAParser = {
        parse: function(rootElement) {

        }
    };
    module.exports = DAParser;
});

